const scrollLeft = {
    mechanicsFunction: (state) => {

    },
    modes: ['playAdventure'],
};

const mechanics1 = {
    mechanicsFunction: (state) => {
        return { state, }
    },
    modes: [],
};




export default {
    scrollLeft,
}