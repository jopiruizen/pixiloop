const styles = (theme) => ({
    footer: {
      backgroundColor: '#EAEAEA',
      minHeight: '60px',
      bottom: '0px',
      position: 'fixed',
      width: '100%',
    },

  });
  
  export default styles;
  